{
    "scripts": {
        "build": "esbuild index.mjs --bundle --outfile=dist/out.js --format=esm --platform=browser --define:global=globalThis",
        "clean": "rimraf dist node_modules"
    },
    "devDependencies": {
        "esbuild": "0.25.2",
        "rimraf": "^6.0.1"
    },
    "dependencies": {
        "jimp": "^1.6.0"
    }
}
