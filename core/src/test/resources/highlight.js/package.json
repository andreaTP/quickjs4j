{
    "scripts": {
        "build": "esbuild index.mjs --bundle --outfile=dist/out.js --format=esm",
        "clean": "rimraf dist node_modules"
    },
    "devDependencies": {
        "esbuild": "0.25.2",
        "rimraf": "^6.0.1"
    },
    "dependencies": {
        "highlight.js": "^11.11.1"
    }
}
